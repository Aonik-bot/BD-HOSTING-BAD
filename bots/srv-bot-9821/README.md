# AONIK Cloud Server Instance

Hosted via **AONIK HOSTING CLOUD PANEL**.

### Quick Start:
1. Edit `main.py` or upload your files.
2. Ensure `requirements.txt` contains your packages.
3. Click **START** in the console panel.

### Commands:
- `python main.py`
- `pip install -r requirements.txt`
